<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for?(:title) ? yield(:title) : "Moises Torres Portafolio" %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

   <body class="m-0 min-h-screen flex flex-col bg-gray-50 text-gray-900">
    <header class="border-b bg-white">
      <div class="mx-auto max-w-6xl px-4 py-4 flex items-center justify-between">
        <%= link_to "Moises Torres", root_path, class: "text-lg font-bold" %>

        <nav class="flex items-center gap-6 text-sm">
          <%= link_to "Experiencia", experience_path, class: "hover:text-indigo-600" %>
          <%= link_to "Stack", stack_path, class: "hover:text-indigo-600" %>
          <%= link_to "About", about_path, class: "hover:text-indigo-600" %>
          <%= link_to "Contacto", contact_path, class: "hover:text-indigo-600" %>
          <%= link_to "GitHub", "https://github.com/loldlm1", target: "_blank", class: "hover:text-indigo-600" %>

          <% if user_signed_in? %>
            <%= link_to "Nuevo Proyecto", new_web_project_path,
                  class: "px-3 py-1.5 rounded-lg bg-indigo-600 text-white hover:bg-indigo-700" %>
            <div class="flex items-center gap-3 text-sm">
              <span class="text-gray-600">Sesión: <%= current_user.email %></span>
              <%= link_to "Salir", destroy_user_session_path,
                    data: { turbo_method: :delete, turbo_confirm: "¿Cerrar sesión?" },
                    class: "px-3 py-1.5 rounded-lg border border-gray-300 hover:bg-gray-100" %>
            </div>
          <% end %>
        </nav>
      </div>
    </header>

    <main class="flex-1">
      <div class="mx-auto max-w-6xl px-4 py-8">
        <%= render "shared/flash" %>
        <%= yield %>
      </div>
    </main>

    <footer class="mt-auto w-full border-t bg-white">
      <div class="mx-auto max-w-6xl px-4 py-6 text-sm text-gray-500 text-center">
        © <%= Time.current.year %> Moises Torres Portafolio — Rails + Tailwind v4
      </div>
    </footer>

  </body>
</html>
